export const metadata = {
  title: "Place Order",
  description: "Place an order for web development, mobile app, UI/UX design, or digital marketing services from OMYTECH.",
  robots: {
    index: false,
    follow: true,
  },
};

export default function OrderLayout({ children }) {
  return children;
}
